spring:
  profiles:
    active: local

hello-world: default-test-world
---
spring:
  config:
    activate:
      on-profile: local

hello-world: local-test-world
---
spring:
  config:
    activate:
      on-profile: dev

hello-world: dev-test-world
---
spring:
  config:
    activate:
      on-profile: qa

hello-world: qa-test-world